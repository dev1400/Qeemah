<mvc:View
	controllerName="sap.ui.demo.tdg.view.Detail"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m">
	<Page
		showNavButton="{device>/isPhone}"
		navButtonPress="onNavBack"
		class="sapUiFioriObjectPage"
		title="{i18n>detailTitle}">
		<content>
		<Label text="{OrderDetailModel>/Address}" design="Bold" />
		
		<!-- <Label text="Select Time" design="Bold" />
		
			<List
				id="list"
				items="{OrderDetailModel>ContactName}"
				mode="{device>/listMode}"
				noDataText="{i18n>masterListNoDataText}"
				select="onSelect"
				growing="true"
				growingScrollToLoad="true">
				<items>
					<ObjectListItem
						type="{device>/listItemType}"
						title="{OrderDetailModel>ContactName}"
						>
					</ObjectListItem>
				</items>
			</List> -->
			<!-- <IconTabBar
				select="onDetailSelect"
				expandable="false"
				id="idIconTabBar">
				<items>
					<IconTabFilter
						binding="{Supplier}"
						key="supplier"
						text="{i18n>iconTabFilterSupplier}"
						icon="sap-icon://supplier">
						<content>
							<core:Fragment fragmentName="sap.ui.demo.tdg.view.SupplierAddressForm" type="XML" />
						</content>
					</IconTabFilter>
					<IconTabFilter
						binding="{Category}"
						key="category"
						text="{i18n>iconTabFilterCategory}"
						icon="sap-icon://hint">
						<content>
							<core:Fragment fragmentName="sap.ui.demo.tdg.view.CategoryInfoForm" type="XML" />
						</content>
					</IconTabFilter>
				</items>
			</IconTabBar> -->
		</content>
		<footer>
			<Toolbar>
			</Toolbar>
		</footer>
	</Page>
</mvc:View>